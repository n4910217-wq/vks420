export interface NavLink {
  label: string;
  path: string;
}

export interface ServiceItem {
  title: string;
  description: string;
  benefits: string[];
}

export interface ContactInfo {
  address: string;
  phone: string;
  phoneDisplay: string;
  whatsappUrl: string;
  telegramUrl: string;
}
